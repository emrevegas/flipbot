TOKEN=your_discord_bot_token
PREFIX=.
OWNER_ID=your_discord_user_id
POINTS_PER_USD=100

# Luci file queue — iki ayrı script:
#   scripts/flipbot_withdraw_worker.lua  (display box withdraw)
#   scripts/flipbot_donation_listener.lua (home donation → deposit_inbox)
# LUCI_QUEUE_DIR=data/luci
# Rakeback tiers are managed via /panel rakeback (stored in DB). No env var needed.
# Affiliate commission is fixed at 10% of (daily deposits - withdrawals). No env var needed.
